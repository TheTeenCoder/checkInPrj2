import { atom } from "jotai";

export const classesAtom = atom([]);
export const studentsAtom = atom([]);
export const focusAtom = atom(null);
export const loadingAtom = atom(false);
export const weekAtom = atom(0);
export const currentWeekAtom = atom(null);
export const classIdAtom = atom(null);
